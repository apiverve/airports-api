name: apiverve_airports
description: Airports is a simple tool for getting airport data. It returns the airport name, location, and more.
version: 1.1.13
homepage: https://apiverve.com/marketplace/airports?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/airports-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
